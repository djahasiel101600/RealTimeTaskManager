
# Example environment file for local development and docker-compose
# Copy this to `.env` and edit secrets before running in production.

# Database (Postgres)
DATABASE_URL=postgresql://taskadmin:taskpass123@postgres:5432/taskmanager
DB_PASSWORD=taskpass123
POSTGRES_DB=taskmanager
POSTGRES_USER=taskadmin

# Redis
REDIS_PASSWORD=redispass123
REDIS_URL=redis://:redispass123@redis:6379/0

# Django / Application
SECRET_KEY==8ra6u@%k@l!)+i00&n&5a9upmcf&q^kyhs_+p=w_dsuid$4+4
# Set to 1 to enable Django DEBUG mode (do not enable in production)
DEBUG=0
# Comma-separated list
ALLOWED_HOSTS=localhost,127.0.0.1,backend,nginx
# Comma-separated origins used by CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://frontend:3000

# Email (optional)
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Frontend (Vite) environment variables
# These are passed as build args and runtime envs to the frontend service
VITE_API_URL=/api
VITE_WS_URL=
REACT_APP_ENV=production

# Optional: override default DB connection host/port if not using DATABASE_URL
# POSTGRES_HOST=postgres
# POSTGRES_PORT=5432

